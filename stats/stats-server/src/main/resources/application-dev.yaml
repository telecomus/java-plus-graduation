server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:6542/statistics
    username: user
    password: "12345"
    sql:
      init:
        mode: always

  hibernate:
    show_sql: true
    jdbc:
      time_zone: UTC

stat-server:
  uri: http://localhost:9090